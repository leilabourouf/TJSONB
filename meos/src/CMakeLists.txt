add_subdirectory(temporal)
add_subdirectory(geo)
if(CBUFFER)
  add_subdirectory(cbuffer)
endif()
if(JSONB)
  add_subdirectory(jsonb)
endif()
if(NPOINT)
  add_subdirectory(npoint)
endif()
if(POSE)
  add_subdirectory(pose)
endif()
if(RGEO)
  add_subdirectory(rgeo)
endif()
